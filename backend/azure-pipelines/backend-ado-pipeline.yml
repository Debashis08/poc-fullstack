trigger:
  branches:
    include:
      - main
      - release/*
      - feature/*
  paths:
    include:
      # Updated to reflect the new structure
      - src/*
      - azure-pipelines/*

variables:
  azureSubscription: 'AzureFreeTrialConnection'
  functionAppName: 'func-fullstack-poc-dev'
  buildConfiguration: 'Release'

# Using your local self-hosted agent
pool: 'Default'

steps:
- task: UseDotNet@2
  displayName: 'Install .NET SDK'
  inputs:
    packageType: 'sdk'
    version: '10.x'

- task: DotNetCoreCLI@2
  displayName: 'Restore NuGet Packages'
  inputs:
    command: 'restore'
    projects: 'src/**/*.csproj'

- task: DotNetCoreCLI@2
  displayName: 'Build the Function App'
  inputs:
    command: 'build'
    projects: 'src/Ecommerce.Functions/Ecommerce.Functions.csproj'
    arguments: '--configuration $(buildConfiguration)'

# Note: The test task was removed because a test project is absent from your current tree. 
# Re-add it pointing to 'src/**/*.Test.csproj' when you scaffold your xUnit project.

- task: DotNetCoreCLI@2
  displayName: 'Publish the Function App'
  inputs:
    command: 'publish'
    publishWebProjects: false
    projects: 'src/Ecommerce.Functions/Ecommerce.Functions.csproj'
    arguments: '--configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)'
    zipAfterPublish: true

- task: AzureFunctionApp@2
  displayName: 'Deploy to Azure Functions'
  inputs:
    connectedServiceNameARM: '$(azureSubscription)'
    appType: 'functionApp'
    appName: '$(functionAppName)'
    package: '$(Build.ArtifactStagingDirectory)/**/*.zip'